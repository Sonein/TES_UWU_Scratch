install node.js https://nodejs.org/en
restart your pc
cd to the directory of the project
open the console
type "npm install vite"
periodically use "npm audit fix" in case u are prompted to do so
"npm install blockly"
"npm install pixi.js"
"npm install @blockly/field-angle"
"npm run dev"
now simply open http://localhost:5173/ in your browser of choice